pipeline {
	agent any

	stages {
  	  stage('Unit Tests') {
		steps {
			#sh 'ant'
		}
	  }
	  stage('Build') {
		steps {
			#junit ‘reports/*.xml’
		}
	  }
	  stage('Deploy') {
		steps {
			#junit ‘reports/*.xml’
		}
	  }
	  stage('Report') {
		steps {
			#junit ‘reports/*.xml’
		}
	  }	  
	}
}
